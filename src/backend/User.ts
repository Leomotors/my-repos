// * User.ts : User

import { User } from "./types";

export const DefaultUser = {
  login: "Loading",
  avatar_url: "../assets/logo.png",
  html_url: "#",
  bio: "Loading",
  public_repos: 0,
} as User;
